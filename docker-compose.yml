version: '3.8'

services:
  app:
    build: .
    container_name: url-shortener-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
